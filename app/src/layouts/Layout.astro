---
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
import '@styles/global.css';
import { getLang, type Language } from '@i18n/index';
import { normalizeBase } from '@utils/url';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

// import.meta.env.BASE_URL can lack a trailing slash when BASE_PATH is supplied
// without one (e.g. BASE_PATH=/website-tunadao). We normalise it to always have
// a trailing slash so that asset URL joins are always correct.
const normBase = normalizeBase(import.meta.env.BASE_URL);
const siteOrigin = (Astro.site?.href ?? '').replace(/\/+$/, '');

// Build an absolute URL for a root-level asset (favicon, og-image, …) by
// joining the normalised base with the filename against the site origin.
// e.g. baseAsset('favicon.svg') → 'http://loureiro.me/website-tunadao/favicon.svg'
const baseAsset = (file: string): string => `${siteOrigin}${normBase}${file}`;

const {
  title,
  description = 'Tunadão 1998 - Tuna do Instituto Politécnico de Viseu. Desde 1998 a representar a academia e a cidade de Viseu.',
  image = baseAsset('og-image.jpg'),
} = Astro.props;

const lang: Language = getLang(Astro.currentLocale, Astro.url);
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang={lang === 'en' ? 'en' : 'pt'} data-lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <!-- Canonical -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href={baseAsset('favicon.svg')} />
    <link rel="apple-touch-icon" sizes="180x180" href={baseAsset('apple-touch-icon.png')} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:locale" content="pt_PT" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={Astro.url} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={image} />

    <!-- Theme Color -->
    <meta name="theme-color" content="#29166f" />

    <!-- Preconnect to external resources -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Font -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Montserrat:wght@600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <!-- Prevent FOUC on theme -->
    <script is:inline>
      const theme =
        localStorage.getItem('theme') ||
        (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      document.documentElement.setAttribute('data-theme', theme);
    </script>
  </head>
  <body>
    <a href="#main-content" class="skip-link">Ir para conteúdo principal</a>

    <Header lang={lang} />

    <main id="main-content">
      <slot />
    </main>

    <Footer lang={lang} />
  </body>
</html>
