{
  "name": "tunadao-monorepo",
  "type": "module",
  "version": "1.0.0",
  "description": "Monorepo for Tunadão 1998 website",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "app",
    "cms"
  ],
  "scripts": {
    "app": "npm run dev -w app",
    "cms": "npm run dev -w cms",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "format:fix": "npm run format:fix --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "seed": "npm run seed -w cms",
    "db:reset": "npm run db:reset -w cms",
    "db:reset-and-seed": "npm run db:reset-and-seed -w cms",
    "prepare": "husky"
  },
  "engines": {
    "node": "^18.20.2 || >=20.9.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "happy-dom": "^20.3.1",
    "husky": "^9.1.7"
  }
}
